# Create Virtual Environment

To set up the environment for our chatbot backend, we need to create a virtual environment first. This allows us to manage dependencies and avoid conflicts with other projects.

1. Open your terminal and navigate to the `backend` directory.
2. Run the following command to create a new virtual environment named `chatbot-backend`.