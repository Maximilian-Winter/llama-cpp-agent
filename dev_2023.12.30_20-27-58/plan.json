# Create Directory Structure
mkdir -p backend/data
mkdir -p backend/models

# Install Required Packages
pip install transformers torch tensorboard numpy pandas tqdm

# Plan for Fullstack Chat Bot System

## Backend

### Environment Setup
Create a virtual environment named `chatbot-backend`
